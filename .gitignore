# .gitignore for the Life IO (Bun/Elysia/Kysely) Project

# Dependencies
# Ignore the node_modules directory where dependencies are installed by Bun.
/node_modules

# Environment Variables
# Never commit secrets or environment-specific configuration.
# It's a good practice to commit a .env.example file instead.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build Artifacts & Generated Files
# This project uses a concat.sh script that creates a 'g' file for sharing.
/g.txt

# Logs
# Ignore log files, which can be verbose and environment-specific.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
bun-debug.log*

# Local Data
# Ignore the local Docker volume data if it's ever mapped to a local directory.
db_data/
dist/

# OS-specific files
# Ignore files generated by macOS, Windows, and Linux systems.
.DS_Store
Thumbs.db
.AppleDouble
.LSOverride

# Editor and IDE configuration
# Ignore configuration files from common editors to avoid team conflicts.
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
